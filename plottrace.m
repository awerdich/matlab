plot(DATABASE(1).v_TRACE(1000:3000)); %TRACE is the average of ROI, TRACEDATA is all the pixels overlaid (check first); 
%":" is from beginning to end
%First ":" is time (x-axis)
%second ":" is the value (y-axis)